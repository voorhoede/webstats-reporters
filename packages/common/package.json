{
  "name": "@webstats-reporters/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "graphql-request": "^3.4.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run prettier-format && yarn run lint"
    }
  },
  "scripts": {
    "install": "tsc",
    "lint": "eslint src --ext .ts",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write"
  },
  "devDependencies": {
    "eslint": "^7.20.0",
    "husky": "^5.1.1",
    "prettier": "^2.2.1"
  }
}
